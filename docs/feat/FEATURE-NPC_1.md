# Feature Spec -> First Pass at AI NPC Integration
---

## Requirements
---
- Reorganize the assets in [[assets/_source/craftpix-net-242415-free-schoolgirls-anime-character-pixel-sprite-pack/]] to be accessible in this new assets design
    - Use the above character assets (starting only with Girl_1) as the first NPC to test
- View images in [[assets/ui/inventory/]] to find pieces to build a UI around conversations
    - Use chromium tool when finished to test
- Attach the backend HTTP API in `ai/` to use conversations in the playtest
- Must use chromium to test frontend appeal and functionality of the feature

## Constraints
---
- Only work on one character asset but allow for selection of all character archetypes defined in `ai/`
- Small notifications of quests only in game UI - nothing more than a small notification
- Must correctly use the API in its full intent

